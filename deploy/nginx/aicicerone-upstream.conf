upstream aicicerone {
    server 127.0.0.1:4321;
    keepalive 32;
}

proxy_cache_path /var/cache/nginx/aicicerone levels=1:2 keys_zone=aicicerone:10m
                 max_size=2g inactive=30d use_temp_path=off;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ""      "";
}
