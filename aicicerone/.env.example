# Copiare in `.env` (ignorato da git). Letto a runtime dal server Node.

# --- IA locale (Ollama o server compatibile con l'API nativa Ollama) ---
LLM_URL=http://localhost:11434
# Leggero (VPS su CPU): qwen3:1.7b · più accurato: qwen3:4b (4 GB RAM) · migliore: qwen3:8b (8 GB)
CHAT_MODEL=qwen3:1.7b
# Ricerca web self-hosted (SearXNG, opzionale ma consigliata). Senza, resta la sola Wikipedia.
SEARXNG_URL=http://localhost:8080

# --- Segnalazioni via email (qualsiasi server SMTP: hosting, Outlook, Gmail con app password, Resend SMTP) ---
SMTP_URL=smtps://utente:password@smtp.example.com:465
REPORT_FROM="AiCicerone <segnalazioni@aicicerone.com>"
REPORT_TO=carlo4340@outlook.it,mario@aicicerone.com

# --- Server ---
HOST=0.0.0.0
PORT=4321
